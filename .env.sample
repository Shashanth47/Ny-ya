# Copy to .env and fill the values
# Used by scripts/app.py
TOGETHER_API_KEY=REPLACE_WITH_YOUR_TOGETHER_API_KEY

# Optional: Flask settings (not strictly required when using Gunicorn)
FLASK_ENV=production